<%= form_with scope: :asset, url: assets_path, html: { class: 'd-none' } do |f| %>
  <% current_user&.assets&.clear if current_user&.assets&.any? %>
  <%= f.file_field :file %>
  <%= f.submit 'save' %>
<% end %>
